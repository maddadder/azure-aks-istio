apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: tnt86
spec:
  hosts:
  - "tnt86.com"
  gateways:
  - istio-system/my-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: shopstr.default.svc.cluster.local
        port:
          number: 3000