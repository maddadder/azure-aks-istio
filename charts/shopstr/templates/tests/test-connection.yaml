apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopstr.fullname" . }}-test-connection"
  labels:
    {{- include "shopstr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shopstr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
