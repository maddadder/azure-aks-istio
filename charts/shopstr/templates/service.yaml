apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopstr.fullname" . }}
  labels:
    {{- include "shopstr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopstr.selectorLabels" . | nindent 4 }}
